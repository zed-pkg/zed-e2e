[package]
name = "zed-opto-sync-adoption-check"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Fail-closed Opto-Sync adoption and frozen-install contract validator"
license = "MIT"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"

[workspace]
